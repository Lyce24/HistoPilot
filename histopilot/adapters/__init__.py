"""Reserved integration namespaces; no adapters are implemented or auto-imported.

Keep optional backend dependencies inside each adapter. Importing the domain,
ports, or application must work without those dependencies installed.
"""
